name: fitness_gym_app
description: A new Flutter project
publish_to: none # Remove the single quotes and the space before the colon
version: 0.1.0

environment:
  sdk: ">=3.1.1 <4.0.0"

dependencies:

  permission_handler: ^11.0.0
  flutter:
    sdk: flutter
  
  image_picker: ^0.8.4+4
  
  flutter_svg: ^1.1.6
  video_player: ^2.7.1
  chewie: ^1.7.1

  adobe_xd: ^2.0.0+1   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/videos/squat.mp4


